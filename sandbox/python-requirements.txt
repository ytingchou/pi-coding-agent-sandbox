# Edit this file, then rebuild the image. Nothing is downloaded at session startup.
# Example application packages (not the worker's requirements.txt).
numpy==2.2.6
rich==14.0.0
requests==2.32.5
minio==7.2.20
# sqlite3 is provided by Python itself, not PyPI.
